pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Clone the repository
                git 'https://github.com/nicol144/devops_final_exam_2.git'
                // Add additional build steps here if needed
                // For example, you can use a build tool like Maven or Gradle to build your project
                // sh 'mvn clean install' or sh 'gradle build'
            }
        }
    }
}
